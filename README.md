# MLOPS-Project-CSED2
This project builds an MLOps pipeline (DVC + experiment tracking) for a student progress dataset, defaulting to `G3` as the target and supporting regression or classification based on params.
